---
- name: Ansible playbook for all hosts
  hosts: all
  vars_files:
    - vars/vault.yml
  roles:
    - fedora
  remote_user: ansible
  tags: fedora

- name: Ansible playbook for configuring Reverse Proxy
  hosts: proxy
  vars_files:
    - vars/vault.yml
  roles:
    - proxy
  remote_user: caddy
  tags: proxy
